appId: 'tardisworldfrlauncher'
productName: 'Tardis World FR Launcher'
artifactName: '${productName} ${version}.${ext}'

copyright: 'Copyright © 2018-2022 Daniel Scalzi'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,.eslintrc.json}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'Asdram (Asdram#0778)'
  vendor: 'Daniel Scalzi (https://github.com/dscalzi/)'
  synopsis: 'Tardis World FR Launcher'
  description: 'Launcher officiel du serveur minecraft moddé Tardis World FR'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'